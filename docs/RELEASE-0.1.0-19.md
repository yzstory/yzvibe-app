# 柚子Vibe 0.1.0 (19)

语音按钮缩至 44 × 44，平时只显示麦克风图标；按住时上方显示提示，底栏间距缩至 4 pt。源码提交 `6b92e5b` 已推送 origin/main。

Debug 编译和 Release 归档通过。归档已复制到 Xcode Organizer 的 2026-09-13 目录。

**尚未上传成功。** 命令行上传返回 `Failed to Use Accounts`，桌面控制多次返回 `cgWindowNotFound`，目前无法经 Xcode 界面完成上传。不能据此判断账号退出。恢复桌面访问后可直接分发 build 19，选择 TestFlight Internal Only，无需重新归档。
