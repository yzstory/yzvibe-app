# 柚子Vibe 0.1.0 (30)

## OMP 执行过程

- 连续工具调用收进一张「执行过程」卡片。点开查看步骤，再展开命令、思考内容和子代理任务；空白助手气泡不再占位。
- 保留 OMP 实际返回的思考文本与中间说明；未提供内容时显示等待状态。最终正文通过完整快照同步，修复只收到部分增量文本时手机缺失结尾的问题。
- 等待时显示原生进度动效和耗时；区分执行工具、等待模型、等待子代理、待确认和连接断开。适配减少动态效果和后台暂停。
- 接入 OMP 子代理进度订阅以及 task 工具详情，显示名称、任务、当前动作、工具次数、耗时、重试 / 完成 / 失败 / 中断状态。
- 后台 task 返回后继续等待真实结束事件；子代理更新归属原始工具。高频进度按 250 ms 合并，结束 / 断线时清理运行状态。
- 对 Claude / Codex，包含最终正文的混合工具消息仍直接展示正文，避免分组遮住答案。

实时子代理进度适用于连接器运行的 OMP RPC 会话。独立终端导入是历史快照；早期连接器未保存的思考无法恢复。完整行为见 [OMP 接入](OMP.md)。

## 验证

- 连接器全量 128 项测试通过，包括 8 项新增 OMP 进度回归测试；最后的缓冲收尾与迟到状态保护经 8 项专项测试复核。
- iOS 全量 139 项测试通过；补充其他助手最终正文保护后，7 项分组与状态测试再次通过（当前总计 140 项）。
- 本机 OMP 18.1.20 无模型调用的 RPC 探针：进度订阅返回 `progress`，`get_subagents` 可用。
- iPhone 17 Pro / iOS 26.5 模拟器：19 次调用合并成一张卡，逐层展开、思考内容、子任务状态与计时均已检查。
- Release 归档成功；App / Widget 均为 0.1.0 (30)。
- 本机连接器已在无运行任务、无待审批时重启加载新代码，原 16 台配对设备和推送配置保留。

## 上传

2026-09-14 21:42:30（Asia/Shanghai）上传成功。Xcode 返回 `Upload succeeded.`、`Uploaded YzVibe`、`EXPORT SUCCEEDED`，上传命令退出码 0。

按本次要求到此结束，未查询 Apple 后续处理或测试组可用性。归档：`ios/build/YzVibe-build30.xcarchive`；上传日志：`ios/build/omp-upload30.log`。
